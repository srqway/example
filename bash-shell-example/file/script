# read file line by line
echo "<<read file line by line>>"
while read line           
do
	echo -e "${line}"
done < ~/.bashrc

# list directory files
for file in /tmp/*
do
  echo "${file}"
done

# echo file name
for file in /tmp/*
do
  echo `basename ${file}`
done


